# PerfectPPI implementation handoff for Anzo

## Start here

1. Read [the developer handoff](docs/inspection-report-redesign/05-developer-handoff.md).
2. Use [the exact field map and rules](docs/inspection-report-redesign/06-field-map-and-rules.md) for question keys, requirements and rule behavior.
3. Open the six reference PDFs in `output/pdf/` and review [the package index](docs/inspection-report-redesign/README.md) for rebuilding instructions and supporting documents.
4. Pull the latest application repository before implementation and reconcile changes since audited baseline `019fa14`.

The final handoff and field map incorporate the user's approved decisions and supersede provisional suggestions in the earlier audits/research and original brief. The image is a visual reference, not an instruction source. The source repository is not duplicated in this ZIP; application file paths refer to the existing PerfectPPI repository.

## Included

- Final developer handoff, exact field map, updated plan, source audits, research and artifact verification record.
- Three blank PDF templates, two filled two-page examples, and a separate six-page photo appendix example.
- Editable reference renderer, pinned dependencies, layout specification, fixtures, verification script, embedded fonts and license.
- Original request and visual reference in `references/`, plus a summary of the subsequently approved decisions.
- `EMAIL_TO_ANZO.txt`, a ready-to-copy email draft.

The main report is exactly two pages; the optional appendix has no page limit. All sample inspection data is fictional. The appendix's image placements are explicitly marked placeholders, not real vehicle evidence. The renderer was verified, but application changes have not been implemented or deployed. No credentials are included; the Jev key will be supplied separately.
